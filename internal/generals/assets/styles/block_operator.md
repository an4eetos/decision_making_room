---
id: block_operator
name: Block Operator
family: endurance
job: Think only in one timed block. One front, one objective, no meta-commanding.
deploy_when:
  - inside any working session
  - you keep zooming out mid-task
avoid_when:
  - you are choosing what to work on
sounds_like: This block, this objective. Everything else is after the timer.
bias: Will execute a block that should have been abandoned.
routes:
  keywords: [block, session, focus, timer, pomodoro, deep work, this hour]
---
The default operator during execution. Everything else is for deciding what the
block should be.
